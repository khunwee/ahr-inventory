# คู่มือใช้งาน AHR Maintenance Inventory (ฉบับละเอียด)

ระบบบริหารคลังอะไหล่หน่วยงาน Maintenance — ใช้ผ่านเบราว์เซอร์ในเครือข่ายโรงงาน

---

## 1. เปิด–ปิดโปรแกรม

**เปิด:** ดับเบิลคลิก `run.bat`
- ครั้งแรกจะติดตั้งตัวเอง (2–5 นาที) แล้วเปิดเบราว์เซอร์ที่ `http://localhost:8770`
- เครื่องอื่นในวง LAN เปิดที่ `http://<ไอพีเครื่องเซิร์ฟเวอร์>:8770`

**ปิด (มี 3 วิธี):**
1. กดปุ่ม **⏻ ปิดโปรแกรม** ที่มุมล่างซ้าย (เฉพาะแอดมิน) — วิธีที่แนะนำ
2. ปิดหน้าต่างสีดำ (Command window) ที่เปิดค้างไว้
3. ถ้าหาไม่เจอ ให้ดับเบิลคลิก `stop.bat`

---

## 2. เข้าสู่ระบบครั้งแรก

1. เปิดโปรแกรม → หน้า Login
2. ผู้ใช้ `admin` รหัส `admin123`
3. ระบบ **บังคับตั้งรหัสผ่านใหม่** ทันที (กรอกรหัสปัจจุบัน `admin123` แล้วตั้งรหัสใหม่)
4. บัญชีทดลองอื่น (รหัส `user123`): `leader1` (หัวหน้า), `tech1` (ช่าง)

**สิทธิ์ 4 ระดับ:** viewer (ดูอย่างเดียว) < engineer (เบิกได้) < leader (รับเข้า/สั่งซื้อ/ส่งออก) < admin (จัดการผู้ใช้/ตั้งค่า/นำเข้า)

---

## 3. เริ่มใช้งานด้วยข้อมูลของคุณเอง (นำเข้าครั้งแรก)

โปรแกรมมาพร้อมข้อมูลตัวอย่างของ AHR อยู่แล้ว หากต้องการเริ่มด้วยข้อมูลชุดของคุณ:

1. เข้าเมนู **นำเข้าข้อมูล** (แอดมิน)
2. กด **⤓ ดาวน์โหลด Template (.xlsx)**
3. เปิดไฟล์ กรอก 2 ชีต:
   - ชีต **Items** — อะไหล่แต่ละชิ้น (ต้องมี `item_code` ไม่ซ้ำ)
     คอลัมน์: `item_code, category, description, part_name, part_number, brand, machine_group, uom, box, level, unit_price, on_hand, min_level, reorder_point, lead_time_months`
     - `on_hand` = จำนวนคงเหลือเริ่มต้น · `reorder_point` = จุดที่ต้องสั่งซื้อ
   - ชีต **Machines** — รายชื่อเครื่อง (ใช้เป็น dropdown ตอนเบิก)
4. กลับมาที่หน้า นำเข้าข้อมูล → เลือกไฟล์ → กด **นำเข้าข้อมูล**
   - ปกติจะเพิ่ม/อัปเดตตาม `item_code` (ไม่ลบของเดิม)
   - ติ๊ก **"ล้างข้อมูลเดิมทั้งหมดก่อนนำเข้า"** ถ้าต้องการเริ่มต้นใหม่ทั้งหมดด้วยชุดข้อมูลนี้ (ลบอะไหล่/เครื่อง/การเบิก/PO เดิม)

> ต้องการล้างข้อมูลตัวอย่างทั้งหมดเพื่อเริ่มใหม่: ปิดโปรแกรม แล้วลบไฟล์ `data\inventory.db` จากนั้นเปิด `run.bat` (จะสร้างฐานข้อมูลว่างจากข้อมูลในโฟลเดอร์ `source`) — หรือให้ผมทำเวอร์ชันฐานข้อมูลว่างให้

---

## 4. งานประจำวันของช่าง/วิศวกร (engineer)

### เบิกอะไหล่
1. เมนู **เบิกอะไหล่**
2. หาอะไหล่ได้ 2 วิธี:
   - **ยิงบาร์โค้ด/QR** ที่ช่องบนสุด (ใช้เครื่องสแกน USB) → เพิ่มเข้าตะกร้าอัตโนมัติ
   - หรือ **พิมพ์ค้นหา** บางส่วนก็ได้ (เช่น `valve komatsu`, `MSP0005`) แล้วกด **+ เพิ่ม**
3. ปรับจำนวนในตะกร้า (ถ้าจำนวนเกินคงเหลือจะขึ้นเตือนสีแดง)
4. เลือก **เครื่องที่ซ่อม**, กรอก **ปัญหาที่พบ**, แนบรูป (ถ้ามี)
5. กด **ยืนยันการเบิก & ตัดสต็อก** → ระบบตัดสต็อกอัตโนมัติ + แจ้งเตือนทุกช่องทาง
6. หน้าต่าง **นับของจริงหลังเบิก** เด้งขึ้น — กรอกจำนวนที่นับได้จริง เพื่อให้หัวหน้าตรวจสอบ

### ดูรายการเบิก / กรองตามช่าง
เมนู **รายการเบิก** → เลือก **ช่างผู้เบิก** จาก dropdown เพื่อดูว่าช่างคนนั้นเคยเบิกอะไรไปบ้าง (มีตัวเลขจำนวนใบเบิกต่อคน) หรือติ๊ก "เฉพาะของฉัน" → กด "ดู" เพื่อดูรายละเอียด/รูปแนบ
> ไม่ต้องทำฐานข้อมูลชื่อช่างแยก — ชื่อช่างมาจากบัญชีผู้ใช้ (role: ช่าง/วิศวกร) ที่สร้างในเมนู "ผู้ใช้งาน" อยู่แล้ว
> หน้า "ส่งออก Oracle" ก็เลือกกรองตามช่างได้เช่นกัน (สรุป/CSV/Excel เฉพาะช่างคนนั้น)

---

## 5. งานของหัวหน้า (leader)

- **รับเข้าสต็อก:** เมนู รับเข้า → ค้นหา → ใส่จำนวน → รับเข้า
- **ตรวจนับ (reconcile):** เมนู รายการเบิก → เปิดใบเบิก → เทียบ "ระบบเหลือ" กับ "นับจริง" → กรอก/บันทึกการตรวจนับ (ส่วนต่างจะโชว์)
- **สั่งซื้อ (PO):** เมนู ต้องสั่งซื้อ → ติ๊กเลือกรายการ → กด "สร้างใบสั่งซื้อจากที่เลือก" → ไปหน้า ใบสั่งซื้อ (PO):
  - เปิดใบ → **ส่งออก Excel** หรือ **พิมพ์ PO** หรือ **รับของเข้าสต็อก** (กดครั้งเดียวเข้าสต็อกทุกบรรทัด) หรือ **ยกเลิก**
- **สรุปการเบิก / ส่งออกให้ Oracle:** เมนู ส่งออก Oracle → เลือกช่วงวันที่ (รูปแบบ dd/mm/yyyy) → กด "แสดงสรุป" เพื่อดูสรุปการเบิกรวมต่ออะไหล่บนหน้าจอ (ใช้อ้างอิงตัดสต็อกใน Oracle) → ดาวน์โหลด **CSV / Excel** หรือ **พิมพ์/PDF**
- **ส่งออกฐานข้อมูลสต็อก:** เมนู คลังอะไหล่ → ปุ่ม **⤓ Excel** / **🖶 พิมพ์/PDF** (ตามตัวกรองที่เลือก) และกรองด้วย หมวด/เครื่อง เพื่อค้นหาง่ายขึ้น
- **บันทึกการใช้งาน:** เมนู บันทึกการใช้งาน — เห็นทุกการเข้าระบบ/เบิก/รับ/สั่งซื้อ

---

## 6. งานของแอดมิน (admin)

- **จัดการผู้ใช้:** เมนู ผู้ใช้งาน → เพิ่ม/แก้ไข กำหนดสิทธิ์ เปิด-ปิดบัญชี รีเซ็ตรหัส (ผู้ใช้ใหม่จะถูกบังคับตั้งรหัสเองครั้งแรก)
- **ตั้งค่าแจ้งเตือน:** เมนู ตั้งค่าแจ้งเตือน → ใส่ค่า Discord/Telegram/LINE แล้วกด "ทดสอบส่ง" เพื่อเช็ก
- **แก้ไขอะไหล่รายชิ้น:** เมนู คลังอะไหล่ → กดปุ่ม ✎ ท้ายแถวของอะไหล่ (เฉพาะแอดมิน) → แก้เฉพาะค่าที่ต้องการ (ชื่อ/เบอร์/ยี่ห้อ/หน่วย/ที่เก็บ/ราคา/Min-Max/ROP ฯลฯ) แล้วบันทึก
  - แก้ "คงเหลือ" ได้ด้วย ระบบจะบันทึกเป็นรายการปรับยอด (adjust) ลง ledger ให้ตรวจสอบย้อนได้
  - เปลี่ยนรหัสอะไหล่ได้ (ระบบเช็กไม่ให้ซ้ำ)
- **นำเข้าข้อมูล:** (ดูข้อ 3) — เหมาะกับแก้/เพิ่มทีละหลายรายการพร้อมกัน (อัปโหลด template)

---

## 7. สลับภาษา ไทย / อังกฤษ

กดปุ่ม **EN / ไทย** ที่มุมบนขวา เพื่อสลับภาษาทั้งหน้าจอ ระบบจำค่าไว้ให้อัตโนมัติ
(ข้อมูลอย่างรหัสอะไหล่/ชื่อยังคงเดิม เปลี่ยนเฉพาะป้ายเมนู/หัวข้อ/ปุ่ม)

## 8. วิเคราะห์ & วางแผนสั่งซื้อด้วย Optimization (leader)

เมนู **วางแผนสั่งซื้อ (Optimize)** มี 5 โหมดวิเคราะห์ให้เลือก (เลือกโหมด → กรอกงบ → กด "คำนวณ"):

1. **ครอบคลุมของด่วนสูงสุดในงบ** (knapsack) — งบเท่านี้ ควรสั่งอะไรกี่ชิ้นให้ครอบคลุมของด่วน (critical) มากที่สุด
2. **ป้องกันเครื่องให้ได้มากสุด** (max-coverage) — เลือกอะไหล่ให้ "เครื่องจักรมีของครบพร้อมซ่อม" ได้มากที่สุดในงบ (แสดงจำนวนเครื่องที่ป้องกันได้ครบ)
3. **กระจายงบให้ทุกเครื่องสมดุล** (max-min fairness) — ไม่ให้เครื่องไหนถูกทิ้ง โดยดันความครอบคลุมของเครื่องที่แย่สุดให้สูงที่สุด
4. **งบขั้นต่ำเพื่อความปลอดภัย** — คำนวณงบน้อยที่สุด + รายการที่ต้องสั่งเพื่อเติมของด่วนทุกตัวให้ปลอดภัย
5. **แนะนำค่า Min/Max ที่เหมาะสม** — คำนวณจุดสั่งซื้อ (ROP) และระดับสูงสุด (Max) ที่เหมาะสมจากอัตราการใช้ + lead time + safety stock (บริการ ~95%) แล้วกด "บันทึกค่า Min/Max ที่แนะนำ" เพื่ออัปเดตทั้งระบบ
6. **วิเคราะห์ ABC / Pareto** — จัดกลุ่มอะไหล่ตามมูลค่าสต็อก (A=มูลค่าสูงสุด ~80%, B, C) เพื่อโฟกัสการควบคุมของกลุ่ม A

ทุกโหมดแสดง KPI + กราฟ + ตาราง และกด **สร้าง PO จากผลลัพธ์** ได้ (ยกเว้นโหมด ABC ที่เป็นการวิเคราะห์)

**ตัวแก้ปัญหา 3 ชั้นอัตโนมัติ** — MiniZinc (ถ้าติดตั้ง) → PuLP/CBC (มากับโปรแกรม) → Greedy (สำรอง)

> MiniZinc ยังทำได้อีก เช่น การจัดตารางสั่งซื้อหลายงวด (lot-sizing) จากอัตราการใช้จริง, การจัดสรรพื้นที่จัดเก็บ (bin-packing), การรวมคำสั่งซื้อตามผู้ขาย — เพิ่มได้เมื่อมีข้อมูลการใช้งานสะสมมากพอ

**ทำไมบางครั้งเห็นแต่ PuLP/CBC?** เพราะเครื่องยังไม่ได้ติดตั้ง MiniZinc — หน้า Optimize จะบอกสถานะให้ (ป้าย "MiniZinc พร้อมใช้" / "ไม่พบ")

**ติดตั้ง MiniZinc ให้ระบบใช้ (ไม่บังคับ):**
1. ดาวน์โหลด MiniZinc จาก https://www.minizinc.org/software.html แล้วติดตั้ง
2. เพิ่มโฟลเดอร์ที่มี `minizinc.exe` ลงใน PATH **หรือ** ใส่พาทเต็มใน `.env`:
   `MINIZINC_PATH=C:\Program Files\MiniZinc\minizinc.exe`
3. เปิดโปรแกรมใหม่ — หน้า Optimize จะขึ้น "MiniZinc พร้อมใช้" และใช้ MiniZinc ให้อัตโนมัติ

> ถ้าไม่ติดตั้ง ระบบใช้ PuLP/CBC (ILP) ที่มากับโปรแกรมแทน ผลลัพธ์ก็เหมาะสมเช่นกัน

## 9. แจ้งเตือน & แดชบอร์ด

ช่องวันที่ทุกหน้าใช้รูปแบบ **dd/mm/yyyy** และมี **ปุ่มปฏิทิน 📅** ให้กดเลือกวันได้สะดวก

**แดชบอร์ดผู้บริหาร** (เลื่อนลงในหน้าแดชบอร์ด) มีกราฟภาพรวมเพิ่ม: สถานะสต็อก (สัดส่วนที่ต้องสั่ง), มูลค่าการเบิกรายเดือน 6 เดือน, เครื่องที่เบิกมากสุด 30 วัน, และสถานะใบสั่งซื้อ (PO) — เพื่อ Monitor ภาพรวมได้เร็ว


- **Real-time:** จุดเขียว "real-time" มุมบนขวา = เชื่อมต่ออยู่ ทุกการเบิก/รับอัปเดตทันที
- **สรุปสิ้นวัน:** ส่งอัตโนมัติเวลา 18:00 ไปยัง Discord/Telegram/LINE
- **แดชบอร์ด:** มูลค่าคงคลัง, ของต้องสั่งด่วน, การเคลื่อนไหว 14 วัน, มูลค่าตามหมวด, ของเบิกมากสุด, **Stock Aging + ของตาย (Dead Stock)**

---

## 10. เบิกผ่าน LINE (ต้องตั้งค่าโดยแอดมิน)

1. แอดมินสร้าง LINE Official Account + เปิด Messaging API แล้วใส่ Token/Secret ในเมนู ตั้งค่าแจ้งเตือน
2. ตั้ง Webhook URL = `https://<โดเมนสาธารณะ>/line/webhook` (เปิดออกเน็ตผ่าน Cloudflare Tunnel/ngrok)
3. ผู้ใช้: หน้าเว็บ → **ผูกบัญชี LINE** → รับรหัส 6 หลัก → พิมพ์ในแชท `ผูก <รหัส>`
4. เบิกผ่าน LINE: พิมพ์ **เบิก** → เลือกอะไหล่/จำนวนด้วยปุ่ม → เลือกเครื่อง → ยืนยัน → (ส่งรูปตามได้)

---

## 11. QR ต่อชิ้น

- เมนู คลังอะไหล่ → กด 🏷️ ท้ายแถว (พิมพ์ป้ายทีละชิ้น) หรือ "🏷️ พิมพ์ QR (ผลลัพธ์)" (พิมพ์ทั้งหน้า)
- นำ QR ไปติดที่ชั้นวาง → ตอนเบิกใช้เครื่องสแกนยิงที่ช่อง "ยิงบาร์โค้ด/QR"

---

## 12. สำรองข้อมูล

ระบบสำรองฐานข้อมูลอัตโนมัติทุกคืน (01:00) เก็บไว้ในโฟลเดอร์ `backups/` (เก็บย้อนหลัง 30 วัน)
คัดลอกโฟลเดอร์ `data/` และ `backups/` ไปเก็บที่อื่นเป็นระยะเพื่อความปลอดภัย
