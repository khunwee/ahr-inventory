# คู่มือติดตั้ง AHR Inventory บน GitHub + Neon + Render (ฉบับละเอียด)

นำระบบขึ้นอินเทอร์เน็ตฟรี ใช้งานได้จากทุกที่ผ่านลิงก์ `https://...onrender.com`
ข้อมูลเก็บถาวรที่ฐานข้อมูล Neon (ไม่หายเมื่อเซิร์ฟเวอร์รีสตาร์ท)

> ✅ โปรแกรมเวอร์ชันนี้ **ผ่านการทดสอบกับ PostgreSQL จริงครบ 28 ฟังก์ชัน** แล้ว (Neon ใช้ PostgreSQL)
> ไม่ต้องแก้โค้ดเพิ่ม — ทำตามขั้นตอนในคู่มือนี้ได้เลย

---

## ภาพรวม: 3 บริการทำหน้าที่อะไร

| บริการ | หน้าที่ | เปรียบเทียบ |
|---|---|---|
| **GitHub** | เก็บไฟล์โปรแกรม (โค้ด) | "ตู้เก็บโปรแกรม" |
| **Neon** | ฐานข้อมูล PostgreSQL เก็บข้อมูลถาวร (อะไหล่, การเบิก, ผู้ใช้, รหัสผ่าน) | "ตู้เก็บข้อมูล" |
| **Render** | ดึงโปรแกรมจาก GitHub มารัน แล้วเชื่อมกับ Neon ให้เปิดใช้ผ่านเว็บ | "เครื่องเซิร์ฟเวอร์" |

**ลำดับการทำ:** GitHub → Neon → Render (ใช้เวลารวมประมาณ 30–45 นาที)

**สิ่งที่ต้องเตรียม:**
- อีเมล 1 บัญชี (แนะนำ Gmail ของหน่วยงาน)
- ไฟล์ `AHR-Inventory.zip` แตกไฟล์ไว้แล้ว (จะได้โฟลเดอร์ `ahr_inventory`)
- เบราว์เซอร์ Chrome หรือ Edge
- **ไม่ต้องใช้บัตรเครดิต** ทั้ง 3 บริการ

---

# ส่วนที่ 1: GitHub — เก็บไฟล์โปรแกรม

### 1.1 สมัครบัญชี GitHub
1. เข้า **https://github.com** → กด **Sign up**
2. กรอกอีเมล → ตั้งรหัสผ่าน → ตั้ง Username (เช่น `ahr-maintenance`)
3. ยืนยันตัวตน (puzzle) → ใส่รหัสยืนยันที่ส่งไปทางอีเมล
4. เลือกแพ็กเกจ **Free**

### 1.2 สร้าง Repository (ที่เก็บโปรแกรม)
1. มุมบนขวา กดปุ่ม **+** → **New repository**
2. กรอก:
   - **Repository name:** `ahr-inventory`
   - **Description:** (ไม่บังคับ) `AHR Maintenance Inventory`
   - เลือก **🔒 Private** ← **สำคัญมาก!** (โฟลเดอร์ `source` มีข้อมูลอะไหล่และราคาของบริษัท ห้ามเปิดเป็น Public)
   - **ไม่ต้อง**ติ๊ก "Add a README file"
3. กด **Create repository**

### 1.3 อัปโหลดไฟล์โปรแกรม (วิธีง่าย — ผ่านเว็บ)
1. ในหน้า repository ที่เพิ่งสร้าง กดลิงก์ **uploading an existing file**
2. เปิดโฟลเดอร์ `ahr_inventory` ในเครื่อง → **เลือกไฟล์และโฟลเดอร์ทั้งหมดข้างใน** (Ctrl+A)
   แล้ว **ลากมาวาง** ในหน้าเว็บ GitHub
   - ⚠️ ลาก "ของข้างใน" โฟลเดอร์ ไม่ใช่ลากตัวโฟลเดอร์ `ahr_inventory` ทั้งก้อน
     (ต้องให้ไฟล์ `requirements.txt`, `Dockerfile` อยู่ชั้นนอกสุดของ repo)
   - **ห้าม**อัปโหลดโฟลเดอร์ `.venv` และไฟล์ `.env` (ถ้ามี) — มีรหัสลับ/ไฟล์ขนาดใหญ่
3. รอจนไฟล์อัปโหลดครบ (ประมาณ 60 ไฟล์)
4. ด้านล่าง ช่อง **Commit changes** พิมพ์ `first upload` → กด **Commit changes**

### 1.4 ตรวจสอบว่าอัปโหลดถูกต้อง
หน้าแรกของ repo ต้องเห็นไฟล์เหล่านี้อยู่ **ชั้นนอกสุด**:
`app/` · `web/` · `scripts/` · `source/` · `requirements.txt` · `Dockerfile` · `render.yaml` · `.python-version`

> 💡 ไฟล์ที่ขึ้นต้นด้วยจุด (`.gitignore`, `.python-version`) Windows อาจซ่อนไว้
> ถ้าไม่เห็น: ใน File Explorer กด **View → Show → Hidden items** แล้วลากอัปโหลดเพิ่ม

> 💡 **ทางเลือก:** ถ้าอัปโหลดผ่านเว็บไม่สะดวก ใช้โปรแกรม **GitHub Desktop** (desktop.github.com)
> → Clone repo → คัดลอกไฟล์ลงโฟลเดอร์ → กด Commit → Push

---

# ส่วนที่ 2: Neon — สร้างฐานข้อมูลถาวร

### 2.1 สมัครบัญชี Neon
1. เข้า **https://neon.com** (เดิมคือ neon.tech) → กด **Sign up**
2. แนะนำกด **Continue with GitHub** (ใช้บัญชี GitHub จากส่วนที่ 1) — ง่ายที่สุด
3. เลือกแพ็กเกจ **Free**

### 2.2 สร้าง Project (ฐานข้อมูล)
1. กด **Create project** (หรือระบบจะพาไปหน้านี้อัตโนมัติ)
2. กรอก:
   - **Project name:** `ahr-inventory`
   - **Postgres version:** ใช้ค่าเริ่มต้น (ล่าสุด)
   - **Region:** เลือก **AWS Asia Pacific (Singapore)** ← ใกล้ไทยที่สุด เร็วที่สุด
3. กด **Create project**

### 2.3 คัดลอก Connection String (ที่อยู่ฐานข้อมูล)
1. ในหน้า Dashboard ของ project กดปุ่ม **Connect**
2. หน้าต่างจะแสดง **Connection string** หน้าตาประมาณนี้:
   ```
   postgresql://neondb_owner:xxxxxxxx@ep-xxxx-xxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
   ```
3. กดปุ่ม **Copy** (ไอคอนคัดลอก) — ถ้ารหัสผ่านถูกซ่อนเป็น `****` ให้กด **Show password** ก่อนคัดลอก
4. **เก็บข้อความนี้ไว้ใน Notepad ชั่วคราว** — จะใช้ในส่วนที่ 3

> 🔒 Connection string มีรหัสผ่านฐานข้อมูลอยู่ในนั้น **ห้ามแชร์ให้คนอื่น / ห้ามใส่ในโค้ดหรือ GitHub**

> ไม่ต้องสร้างตารางเอง — โปรแกรมจะสร้างตาราง + ข้อมูลเริ่มต้นให้อัตโนมัติตอน Render รันครั้งแรก

---

# ส่วนที่ 3: Render — รันโปรแกรมให้เปิดใช้ผ่านเว็บ

### 3.1 สมัครบัญชี Render
1. เข้า **https://render.com** → กด **Get Started**
2. กด **GitHub** เพื่อสมัครด้วยบัญชี GitHub (สะดวก เพราะต้องเชื่อม repo อยู่แล้ว)
3. กรอกข้อมูลเบื้องต้น → ยืนยันอีเมล

### 3.2 สร้าง Web Service
1. ใน Dashboard กดปุ่ม **+ New** → เลือก **Web Service**
2. ส่วน **Source Code** → เลือก **GitHub**
   - ครั้งแรกจะขอสิทธิ์: กด **Configure GitHub** → เลือก **Only select repositories** → เลือก `ahr-inventory` → **Install**
     (จำเป็น เพราะ repo เป็น Private)
3. เลือก repo **ahr-inventory** → กด **Connect**

### 3.3 กรอกการตั้งค่า (สำคัญ — กรอกให้ตรงทุกช่อง)

| ช่อง | ค่าที่ต้องกรอก |
|---|---|
| **Name** | `ahr-inventory` (จะเป็นส่วนหนึ่งของลิงก์ เช่น ahr-inventory.onrender.com) |
| **Language** | `Python 3` |
| **Branch** | `main` |
| **Region** | **Singapore (Southeast Asia)** ← ให้อยู่ใกล้ Neon |
| **Root Directory** | เว้นว่าง |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python -m scripts.bootstrap && uvicorn app.main:app --host 0.0.0.0 --port $PORT` |
| **Instance Type** | **Free** |

> ⚠️ Start Command ต้องพิมพ์ให้ตรงทุกตัวอักษร (แนะนำคัดลอกจากคู่มือนี้)

### 3.4 ตั้งค่า Environment Variables (ตัวแปรลับ)
เลื่อนลงไปส่วน **Environment Variables** → กด **Add Environment Variable** ทีละรายการ:

| Key | Value |
|---|---|
| `DB_URL` | วาง Connection string จาก Neon (ส่วนที่ 2.3) |
| `SECRET_KEY` | กดปุ่ม **Generate** ข้างช่อง Value (ให้ระบบสุ่มค่ายาว ๆ ให้) |
| `PYTHON_VERSION` | `3.12.7` |
| `TIMEZONE` | `Asia/Bangkok` |

> 💡 ใส่ค่าลับไว้ที่นี่เท่านั้น ไม่ต้องสร้างไฟล์ `.env` บน Render

### 3.5 Deploy
1. กดปุ่ม **Deploy Web Service** ด้านล่างสุด
2. Render จะพาไปหน้า **Logs** — รอประมาณ **3–5 นาที** (ติดตั้งแพ็กเกจ)
3. สังเกตข้อความใน Logs:
   - `[bootstrap] empty database — importing sample dataset...` → กำลังโหลดข้อมูลตัวอย่างเข้า Neon
   - `[bootstrap] import complete`
   - `Uvicorn running on http://0.0.0.0:...` และ **Your service is live 🎉** → สำเร็จ!

### 3.6 เข้าใช้งานครั้งแรก
1. มุมบนซ้ายของหน้า service จะมีลิงก์ เช่น **https://ahr-inventory.onrender.com** → กดเปิด
2. ล็อกอิน **admin / admin123**
3. ระบบบังคับ **ตั้งรหัสผ่านใหม่** → ตั้งรหัสที่ปลอดภัย (ครั้งเดียว อยู่ถาวรใน Neon)
4. เข้าเมนู **ผู้ใช้งาน** → เปลี่ยนรหัส `leader1`, `tech1` หรือ **ปิดใช้งาน** บัญชีทดลอง แล้วสร้างบัญชีจริงของช่าง/หัวหน้า
5. แชร์ลิงก์ให้ทีมใช้งานได้จากทุกที่ (มือถือ/คอมพิวเตอร์) — ไม่ต้องติดตั้งอะไร

> ⚠️ **เปลี่ยนรหัสบัญชีทดลองทันที** — เพราะขึ้นอินเทอร์เน็ตแล้ว ใครรู้ลิงก์ก็เข้าหน้า login ได้

---

# ส่วนที่ 4: การใช้งานและดูแลระบบประจำ

### 4.1 เริ่มใช้ข้อมูลจริงของหน่วยงาน
ระบบโหลดข้อมูลตัวอย่าง AHR ให้ตอนแรก ถ้าต้องการเริ่มด้วยข้อมูลจริง:
เมนู **นำเข้าข้อมูล** → ดาวน์โหลด Template → กรอก → อัปโหลด
→ ติ๊ก **"ล้างข้อมูลเดิมทั้งหมดก่อนนำเข้า"** ถ้าต้องการล้างข้อมูลตัวอย่างออกก่อน

### 4.2 อัปเดตโปรแกรมเวอร์ชันใหม่ (ง่ายกว่าเดิม — ข้อมูลไม่หายแน่นอน)
1. เข้า GitHub repo → **Add file → Upload files**
2. ลากไฟล์เวอร์ชันใหม่ (ของข้างในโฟลเดอร์) มาวางทับ → **Commit changes**
3. Render จะ **deploy ใหม่อัตโนมัติ** ภายในไม่กี่นาที (ดูได้ที่แท็บ Events/Logs)
4. ข้อมูลทั้งหมด + รหัสผ่านอยู่ที่ Neon → **ไม่หาย ไม่ถูกรีเซ็ต** และข้อมูลตัวอย่างจะไม่ถูกโหลดซ้ำ

### 4.3 ดู Log เมื่อมีปัญหา
Render → เลือก service → แท็บ **Logs** — ดูข้อความ error ล่าสุด (ส่งภาพหน้าจอส่วนนี้มาให้ช่วยวิเคราะห์ได้)

### 4.4 Restart โปรแกรม
Render → service → **Manual Deploy → Deploy latest commit** หรือ **Restart service**

### 4.5 สำรองข้อมูล
- Neon Free มีระบบย้อนคืนข้อมูล (restore) ได้ในช่วงเวลาสั้น ๆ เท่านั้น
- **แนะนำ:** ทุกสัปดาห์ให้หัวหน้าดาวน์โหลด **Excel ฐานข้อมูลสต็อก** (เมนูคลังอะไหล่ → ⤓ Excel)
  และ **สรุปการเบิก** (เมนูส่งออก Oracle → Excel) เก็บไว้ในไดรฟ์ของหน่วยงาน

---

# ส่วนที่ 5: ข้อจำกัดของแพ็กเกจฟรี (ควรรู้)

**Render Free**
- เซิร์ฟเวอร์ **"หลับ" เมื่อไม่มีคนใช้ 15 นาที** — คนแรกที่เข้าหลังจากนั้นจะรอหน้าโหลดประมาณ **1 นาที** จากนั้นใช้งานเร็วตามปกติ
- **รูปที่แนบตอนเบิก** เก็บบนดิสก์ชั่วคราวของ Render → **หายเมื่อเซิร์ฟเวอร์หลับ/รีสตาร์ท/deploy ใหม่** (ข้อมูลการเบิกไม่หาย หายเฉพาะไฟล์รูป)
- **สรุปสิ้นวันอัตโนมัติ (18:00)** และ **สำรองรายคืน** จะไม่ทำงานถ้าเซิร์ฟเวอร์หลับอยู่ในเวลานั้น
- โควตาชั่วโมงใช้งานฟรีต่อเดือน (พอสำหรับ 1 service)

**Neon Free**
- พื้นที่เก็บข้อมูล **0.5 GB** — เพียงพอมากสำหรับระบบนี้ (ข้อมูลอะไหล่ 1,500 รายการใช้ไม่ถึง 5 MB)
- ฐานข้อมูลพักตัวเมื่อไม่ใช้งาน 5 นาที และตื่นเร็วมาก (เสี้ยววินาที) — ผู้ใช้แทบไม่รู้สึก
- มีโควตาการประมวลผลต่อเดือน — สำหรับการใช้งานในเวลาทำงานปกติถือว่าเพียงพอ

**ถ้าต้องการใช้งานจริงจังแบบไม่มีข้อจำกัด:** อัปเกรด Render เป็นแพ็กเกจ Starter (ไม่หลับ + เพิ่มดิสก์ถาวรสำหรับรูปได้) หรือใช้ Oracle Cloud Always Free VM (ดูไฟล์ DEPLOY.md)

---

# ส่วนที่ 6: เชื่อม LINE Bot (ข้อดีของการขึ้น Render)

Render ให้ลิงก์ **HTTPS** อยู่แล้ว → **ไม่ต้องใช้ Cloudflare Tunnel/ngrok**
1. ตั้งค่า LINE Messaging API ตามคู่มือหลัก → ใส่ Token/Secret ในเมนู **ตั้งค่าแจ้งเตือน**
2. ใน LINE Developers Console ตั้ง **Webhook URL** = `https://ahr-inventory.onrender.com/line/webhook` → เปิด **Use webhook**
3. ⚠️ ถ้าเซิร์ฟเวอร์หลับอยู่ ข้อความ LINE แรกอาจไม่ได้รับคำตอบ (ระหว่างเซิร์ฟเวอร์กำลังตื่น) — ส่งซ้ำอีกครั้งหลัง 1 นาที

---

# ส่วนที่ 7: แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| Build failed: `Could not open requirements file` | อัปโหลดไฟล์ผิดชั้น — `requirements.txt` ต้องอยู่ชั้นนอกสุดของ repo (ดู 1.3) |
| Logs ขึ้น `could not connect to server` / `password authentication failed` | `DB_URL` ผิด — คัดลอก Connection string จาก Neon ใหม่ (กด Show password ก่อน) แล้วแก้ใน Render → Environment → Save (ระบบ deploy ใหม่เอง) |
| `No open ports detected` / Deploy ค้าง | Start Command ผิด — ต้องมี `--port $PORT` ตามคู่มือ 3.3 |
| เปิดลิงก์แล้วโหลดนาน ~1 นาที | ปกติของ Render Free (เซิร์ฟเวอร์กำลังตื่น) รอสักครู่ |
| ล็อกอิน admin/admin123 ไม่ได้ | เคยเปลี่ยนรหัสแล้ว — ใช้รหัสใหม่ที่ตั้งไว้ (เก็บถาวรใน Neon) |
| รูปแนบใบเบิกหายไป | ข้อจำกัด Render Free (ดูส่วนที่ 5) |
| กราฟ/ฟอนต์ไม่ขึ้น | กด Ctrl+F5 เพื่อล้าง cache เบราว์เซอร์ |
| ข้อมูลตัวอย่างหายไปเอง | ตรวจว่า `DB_URL` ถูกตั้งค่า — ถ้าลืมตั้ง โปรแกรมจะใช้ SQLite ชั่วคราวซึ่งหายเมื่อรีสตาร์ท |

---

## เช็กลิสต์สรุป

- [ ] GitHub: สร้าง repo แบบ **Private** และอัปโหลดไฟล์ (requirements.txt อยู่ชั้นนอกสุด)
- [ ] Neon: สร้าง project Region **Singapore** และคัดลอก Connection string
- [ ] Render: สร้าง Web Service (Free, Singapore) ใส่ Build/Start Command ถูกต้อง
- [ ] Render: ใส่ `DB_URL`, `SECRET_KEY` (Generate), `PYTHON_VERSION=3.12.7`, `TIMEZONE`
- [ ] เปิดลิงก์ → login admin → ตั้งรหัสใหม่
- [ ] เปลี่ยนรหัส/ปิดบัญชีทดลอง และสร้างบัญชีช่างจริง
- [ ] ตั้งเวลาดาวน์โหลด Excel สำรองข้อมูลรายสัปดาห์
